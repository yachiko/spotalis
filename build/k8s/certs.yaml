apiVersion: v1
kind: Secret
metadata:
  name: spotalis-webhook-certs
  namespace: spotalis-system
  labels:
    app.kubernetes.io/name: spotalis
    app.kubernetes.io/instance: spotalis
    app.kubernetes.io/component: webhook
    app.kubernetes.io/managed-by: kubectl
type: Opaque
data:
  # TLS certificate and key will be generated by cert-manager or 
  # injected by the deployment process
  tls.crt: ""
  tls.key: ""
---
# NOTE: This is a placeholder for webhook certificates.
# In production, use cert-manager or similar solution:
#
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: spotalis-webhook-certs
#   namespace: spotalis-system
# spec:
#   secretName: spotalis-webhook-certs
#   issuerRef:
#     name: selfsigned-issuer
#     kind: ClusterIssuer
#   dnsNames:
#   - spotalis-webhook.spotalis-system.svc
#   - spotalis-webhook.spotalis-system.svc.cluster.local
